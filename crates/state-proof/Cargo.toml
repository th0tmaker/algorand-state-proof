[package]
name = "algorand-state-proof"
version = "0.1.0"
edition = "2024"
description = "Cryptographic attestation of Algorand ledger state across a block range — enables trustless, lightweight, quantum-secure verification without running a node."
license = "MIT"

[dependencies]
keccak = { path = "../keccak" }
